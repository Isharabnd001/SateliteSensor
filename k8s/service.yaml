apiVersion: v1
kind: Service
metadata:
  name: gke-tutorial-deployment-4
spec:
  selector:
    app: gke-tutorial-deployment-4
  type: NodePort
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000