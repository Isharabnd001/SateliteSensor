apiVersion: v1
kind: Service
metadata:
  name: satelite-sensor-service
spec:
  clusterIP: None
  selector:
    app: satelite-sensor-deployment
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000